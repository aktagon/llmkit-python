# Code generated by codegen/generate.py; DO NOT EDIT.

from __future__ import annotations

from dataclasses import dataclass, field

from .providers import ProviderName


# Music wire-shape discriminators. Each fully determines the request
# body, the response audio path, AND the byte encoding (base64 vs hex).


@dataclass(frozen=True)
class MusicModelDef:
    model_id: str
    label: str
    supports_lyrics: bool = False
    max_duration_seconds: int = 0
    output_mime: str = ""


@dataclass(frozen=True)
class MusicGenDef:
    # wire_shape is MusicPredict | MusicGenerateContent | MusicMinimax.
    wire_shape: str
    # gen_endpoint is an override; empty = use provider main endpoint.
    gen_endpoint: str = ""
    models: tuple[MusicModelDef, ...] = field(default_factory=tuple)


_MUSIC_GEN: dict[ProviderName, MusicGenDef] = {
    ProviderName.GOOGLE: MusicGenDef(
        wire_shape="MusicGenerateContent",
        gen_endpoint="",
        models=(
            MusicModelDef(
                model_id="lyria-3-clip-preview",
                label="Lyria 3 Clip",
                supports_lyrics=True,
                max_duration_seconds=30,
                output_mime="audio/mpeg",
            ),
            MusicModelDef(
                model_id="lyria-3-pro-preview",
                label="Lyria 3 Pro",
                supports_lyrics=True,
                max_duration_seconds=120,
                output_mime="audio/mpeg",
            ),
        ),
    ),
    ProviderName.MINIMAX: MusicGenDef(
        wire_shape="MusicMinimax",
        gen_endpoint="https://api.minimax.io/v1/music_generation",
        models=(
            MusicModelDef(
                model_id="music-2.6",
                label="MiniMax Music 2.6",
                supports_lyrics=True,
                max_duration_seconds=0,
                output_mime="audio/mpeg",
            ),
        ),
    ),
    ProviderName.VERTEX: MusicGenDef(
        wire_shape="MusicPredict",
        gen_endpoint="",
        models=(
            MusicModelDef(
                model_id="lyria-002",
                label="Lyria 2",
                supports_lyrics=False,
                max_duration_seconds=30,
                output_mime="audio/wav",
            ),
        ),
    ),
}


def music_gen_config(provider: ProviderName) -> MusicGenDef | None:
    return _MUSIC_GEN.get(provider)
